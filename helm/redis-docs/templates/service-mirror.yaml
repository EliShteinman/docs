{{- if .Values.mirror.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-docs.fullname" . }}-mirror
  labels:
    {{- include "redis-docs.labels" . | nindent 4 }}
    app.kubernetes.io/component: mirror
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-docs.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: mirror
{{- end }}
